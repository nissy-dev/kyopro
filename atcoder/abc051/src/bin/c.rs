use proconio::{fastout, input};

#[fastout]
fn main() {
    input! {
        // s: String,
        // n: usize,
        // (n, m): (usize, usize),
        (sx, sy, tx, ty): (usize, usize, usize, usize),
        // data: [usize; n],
        // data: [[usize; m]; n]
    }
}
